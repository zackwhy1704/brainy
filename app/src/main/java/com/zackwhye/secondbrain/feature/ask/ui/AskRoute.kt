package com.zackwhye.secondbrain.feature.ask.ui

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/** No ViewModel yet — stub tab has no state machine until Phase 3. */
@Composable
fun AskRoute(modifier: Modifier = Modifier) {
    AskScreen(modifier = modifier)
}
